{
  "name": "docmap",
  "version": "0.0.1",
  "description": "map style documentations for collaborators",
  "main": "index.js",
  "scripts": {
    "compile": "babel --presets es2015 -d lib/ src/",
    "prepublish": "npm run compile",
    "test": "npm run compile && jest"
  },
  "jest": {
    "testPathIgnorePatterns": [
      "src/",
      "node_modules/"
    ]
  },
  "repository": {
    "url": "https://github.com/aruntk/docmap",
    "type": "git"
  },
  "author": "aruntk",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-preset-es2015": "^6.22.0",
    "eslint": "^3.14.1",
    "eslint-config-airbnb-base": "^11.0.1",
    "eslint-plugin-import": "^2.2.0",
    "jest": "^18.1.0"
  },
  "dependencies": {
    "espree": "^3.3.2"
  }
}
